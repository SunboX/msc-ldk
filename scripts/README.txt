msc_*.sh scripts are taken from msc/0000/msc_linux_scripts/src/

As they provide us the possibility to map external MSC git URLs to an internal GIT cache, we can't "git submodule" or "git clone" them as this would work only on the outside or the inside of the MSC network.

